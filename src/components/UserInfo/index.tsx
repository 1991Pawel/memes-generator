import supabase from "../../config/supabaseClient";
export const getUserData = async (setUserInfo: any) => {
  //   const { data } = await supabase
  //     .from("profiles")
  //     .select("id, username, avatar_url, website");
};
